version: 2.1

orbs:
  terraform: mafuyuk/terraform@0.0.1

workflows:
  deploy:
    jobs:
      - terraform/plan
      - hold:
          type: approval
          requires:
            - terraform/plan
      - terraform/apply:
          requires:
            - hold