version: 2.1

orbs:
  terraform: mafuyuk/terraform@dev:df1e9ba
  aws-cli: circleci/aws-cli@0.1.9

workflows:
  test:
    jobs:
      - terraform/init
      - terraform/plan:
          requires:
            - terraform/init
